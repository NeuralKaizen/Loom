---
type: concept
created: <YYYY-MM-DD>
tags: [<topic>, <topic2>]
context: <one line — what the user was doing when this came up>
---

<!-- Write the section headings below in the USER'S language.
     Use loose lists — a BLANK LINE between every bullet — so the note breathes
     and reads well in Obsidian. Applies to every bulleted list in the note. -->

# <Concept Name>

## What it is
<2-3 lines. The concept, nothing else. No "in this note we will explore". Just the thing.>

## Why it matters here
<Tie it to their actual task. Name what they're doing. Example:
"You're loading the cart from an API and the UI flickers between states.
That's exactly what the optimistic-updates pattern solves.">

## The core
<How it works, with example(s). TECHNICAL concept → a minimal code pattern:

```<lang>
// Use THEIR variable names and stack. Minimal shape, not a full app.
```

NON-TECHNICAL concept → a concrete worked example or analogy instead.
Code is just one kind of example — include it only when it fits.>

## Connections
<[[wikilinks]] to related notes. Link the genuinely related notes that already
exist in the vault, plus a few [[adjacent-concept]] ghost links worth learning
next. Link liberally, but only what is truly related. One line each on the relation:>
- [[related-note]] — <how it connects>

- [[adjacent-concept]] — <why it's worth learning next>

<!-- ## Demo — OPTIONAL. Omit when the note is first written. Add ONLY after the
     user accepts the demo offer and you've written the .html. Link to the standalone
     file (it opens in the browser); do NOT embed via <iframe> — Obsidian strips
     <script>/onclick and sandboxes iframes. When present:

## Demo
▶ **[Open interactive demo](<concept-slug>-demo.html)** — opens in your browser, full JS.
<One line on what the demo lets you do.>
-->

## Why it works this way
- **The reasoning:** <why it's like this>

- **Tradeoff:** <what you give up>

- **When NOT to apply it:** <the boundary — for non-technical concepts, where it breaks down>

## Common mistakes
- <concrete pitfall 1>

- <concrete pitfall 2>

- <concrete pitfall 3>

---
*Generated by everlearn · loom — come back when you need it.*

